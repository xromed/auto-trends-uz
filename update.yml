name: Обновление дашборда

on:
  schedule:
    - cron: '0 4 * * *'   # Каждый день в 09:00 по Ташкенту (UTC+5 = 4:00 UTC)
  workflow_dispatch:        # Позволяет запускать вручную из GitHub

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write       # Нужно для коммита в репо

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python setup
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Установка зависимостей
        run: pip install pytrends "urllib3<2"

      - name: Генерация дашборда
        run: python generate.py

      - name: Коммит и пуш
        run: |
          git config user.name "auto-trends-bot"
          git config user.email "bot@noreply.github.com"
          git add index.html
          git diff --staged --quiet || git commit -m "🔄 Обновление $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
